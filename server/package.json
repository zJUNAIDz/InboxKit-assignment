{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun run tsup src/index.ts --out-dir  dist --minify"
  },
  "dependencies": {
    "hono": "^4.12.15",
    "ioredis": "^5.10.1",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}